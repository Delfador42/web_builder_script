
Change browser tab title

Change Site Title

Change Home page


Line to add at the end:
os.system("cd /root/walkthroughs; docker-compose down; docker-compose up;")


* When you add or delete a pdf run the script


BREAK: if pdf is removed website breaks 
  FIX: run the script


- Add fix to script 
BREAK: if pdf is moved into files directory (This is occastional, if it breaks run the fix)
  FIX: docker-compose down; docker-compose up -d
